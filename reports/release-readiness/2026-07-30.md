# Release readiness — 2026-07-30

## Decision

The current branch is ready for a controlled production deployment after the production
environment checks below are confirmed. This review did not push or deploy any code.

Baseline reviewed:

- Branch: `codex/full-history-model-automation`
- Pre-review commit: `8b1eb57f06d52e9138d2c201ff5ccfc28fc56994`
- Deployment trigger: pushes to `dev` or `main` run `.github/workflows/deploy.yml`

## Automated gates

| Gate | Result |
| --- | --- |
| Backend Ruff | Pass |
| Backend test suite | Pass — 134 tests |
| Backend lock check | Pass — 71 packages resolved |
| Frontend ESLint | Pass — 0 errors, 3 performance warnings |
| Frontend TypeScript | Pass |
| Frontend production build | Pass — 14 static pages generated |
| npm dependency audit | Pass — 0 known vulnerabilities |
| Git whitespace check | Pass |
| Tracked-file credential scan | Pass — no credential patterns found |

The dependency gate required upgrading Next.js and its lint configuration, plus pinning
patched transitive versions of PostCSS, Sharp, minimatch, and brace-expansion.

## Local smoke evidence

The development environment intentionally reported `use_mock_data=true`; these checks
validate request flow and contracts, not live production data:

- API health: HTTP 200
- Slate list: HTTP 200
- Selected DK player pool: HTTP 200, 159 players
- Optimizer: HTTP 200, 5 lineups, deterministic seed 42, approximately 0.6 seconds
- Frontend home page: HTTP 200

## Required pre-deployment checks

Before triggering the deployment workflow:

1. Confirm the production API service sets `APP_ENV=production` and
   `USE_MOCK_DATA=false`.
2. Confirm the data worker can write to `/var/lib/diamscore/cache` and its latest refresh
   is healthy.
3. Back up or record the current deployed commit so rollback is immediate.
4. After deployment, verify `/health` reports production/live data, then load a real slate
   and generate constrained lineups before announcing availability.

## Known non-blocking risks

- ESLint retains three performance warnings: two raw image elements and the inline Google
  Tag Manager script.
- Turbopack reports a broad file-tracing warning involving `lib/blog.ts`; the production
  build succeeds, but the trace should be narrowed in a later performance cleanup.
- The automated historical model report is still `NOT_EVALUATED` until enough qualified
  real pre-lock slate snapshots and post-game results have accumulated. This must not be
  represented as proven model outperformance.
